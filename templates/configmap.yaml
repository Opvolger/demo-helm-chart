apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "demo-helm-chart.fullname" . }}
  labels:
    {{- include "demo-helm-chart.labels" . | nindent 4 }}
data:
  index.php: |
    <?php phpinfo(); ?>
